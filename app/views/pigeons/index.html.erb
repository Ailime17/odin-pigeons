<h1>All pigeons:</h1>
<ul>
  <% @pigeons.each do |p|%>
    <li>
    <%= link_to p.name, p %>
    <%= link_to "Delete", pigeon_path(p), data: {
      turbo_method: :delete,
      turbo_confirm: 'Are u sure?'
      } %>
    </li>
  <% end %>
</ul>


<%= link_to "Add new pigeon", new_pigeon_path %>